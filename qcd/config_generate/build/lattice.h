#include <iostream>
#include <Eigen/Dense>
#include <unsupported/Eigen/CXX11/Tensor>
#include <complex>
using namespace Eigen;

#pragma once

class lattice{
    private:
    MatrixXd gauge_field=;
    public:
    lattice(size);
}